var mongoose = require('mongoose')
// mongoose.connect('mongodb://localhost/express-todo');

var conn = mongoose.connection;

exports.createUser = function (req, res) {
	// TODO: Save the user to the database (and return?)
};